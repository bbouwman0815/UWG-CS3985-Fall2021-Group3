Title: Medical Management System
Purpose: This application is used for medical personel to manage their patients. The application have three roles - Medical Personnel, Patient, and Caregiver;
they share patient's data to care the patient and give suggestion on medical conditions for patient.

SETUP:

BEGIN
	Download the project and point to 'code' fold, then git bash 'code' folder

SETUP
	run 'bash setup.sh setup' to set up and install the JeroMQ jar into your local Maven repository.

COMPILE
	run 'bash setup.sh compile' to compile your project

TEST
	run 'bash setup.sh test' to test all your project tests

EXECUTE
	`run 'bash setup.sh run' to execute the project and bring you to login window
